name: Hello World

on: push


jobs:
 greet:
  runs-on: ubuntu-latest

  steps:
   - name: Say Hello
     run: echo "Hello from github actions via ACT"
   - name: Show Date 
     run: date 
   - name: Show Directory
     run: pwd 
